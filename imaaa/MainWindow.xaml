<Window x:Class="TypingTrainer.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Тренажер скорописи" Height="300" Width="400">
    <Grid Margin="10">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <!-- Текст для набора -->
        <TextBlock x:Name="TextBlock_Task" Grid.Row="0" Text="Нажмите Старт" Margin="0,0,0,10" TextWrapping="Wrap"/>

        <!-- Поле ввода -->
        <TextBox x:Name="TextBox_Input" Grid.Row="1" TextWrapping="Wrap" AcceptsReturn="True" IsEnabled="False"/>

        <!-- Кнопки -->
        <StackPanel Grid.Row="2" Orientation="Horizontal" Margin="0,10,0,0">
            <Button x:Name="Button_Start" Content="Старт" Width="100" Click="Button_Start_Click"/>
            <Button x:Name="Button_Reset" Content="Сброс" Width="100" Margin="10,0,0,0" Click="Button_Reset_Click"/>
        </StackPanel>

        <!-- Результат -->
        <TextBlock x:Name="TextBlock_Result" Grid.Row="3" Text="Скорость: 0 зн/мин" Margin="0,10,0,0" FontWeight="Bold"/>
    </Grid>
</Window>